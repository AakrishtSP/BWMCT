import React from "react";
import Card from "../../UI/Card";

const GameList = (props) => {
    console.log(props);
    return (
        <div>
            <Card>test 1</Card>
        </div>
    );
};

export default GameList;
